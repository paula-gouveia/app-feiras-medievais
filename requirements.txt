# Scraper (Fase 1)
requests==2.32.3
truststore>=0.10.0
beautifulsoup4==4.12.3
# lxml removido: incompatibilidade entre lxml>=6 e bs4 4.12.x em HTML com
# namespaces XML (ValueError em _getNsTag). Usamos html.parser (built-in).
geopy==2.4.1

# Base de dados (Fase 2)
supabase>=2.0.0
python-dotenv>=1.0.0
